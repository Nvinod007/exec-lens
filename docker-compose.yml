services:
  exec-lens:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MAX_SNIPPET_LENGTH=20000
      - RUNNER_TIMEOUT_MS=10000
      - MAX_RUNNER_STEPS=5000
    mem_limit: 512m
